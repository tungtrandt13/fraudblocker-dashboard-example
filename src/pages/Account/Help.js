import React from "react";
import styles from "./Account.module.scss";

const Help = () => {
    return (
        <div className={styles.content}>
            <h1> Help </h1>
        </div>
    );
};

export default Help;
