import React from "react";
import { Typography, Box } from "@mui/material";
import styles from "./Account.module.scss";

const Terms = () => {
    return (
        <Box className={styles.content}>
            <Typography variant="h1">Terms & Policies</Typography>
        </Box>
    );
};

export default Terms;
